/*
=====/=====/=====/=====/=====/=====/=====/=====/=====/=====


=====/=====/=====/=====/=====/=====/=====/=====/=====/=====
*/
#include<stdio.h>

int inicial, correto = 0, suposto;
char resultado;


int obter_valor(){
  
  
  }
  
}

int main(){

 
  
  printf("O valor que você pensou é: %d \n", inicial);
  scanf("%c", &resultado);

  if(resultado == ("sim" || "SIM" || "Sim" || "yes" || "s")){
    printf("De primeira, o pai é brabo\n");
  }

    
  else if(resultado == ("não" || "NÃO" || "Nao" || "nao" || "n")){
    printf("Seu valor é maior ou menor que o palpite? \n");
    scanf("%c", &resultado);
    
    if(resultado == ("maior" || "Maior" || "MAIOR")){ // Valor maior
    // FUNÇÃO

      while(correto == 0){
      suposto = inicial * 2; //  Suposto sendo o dobro
      printf("Então o seu valor é maior ou menor que %d?", suposto);
      scanf("%c", &resultado);
      
        if(resultado == "igual"){ // Valor maior
          correto = 1;
      // FUNÇÃO
          else if(resultado == "menor")
          
        }

          
  }
    else if(resultado == ("menor" || "Menor" || "MENOR")){
      // Função 2
    }

    
  }
  

  return 0;
}

