/*
Escreva um programa que gera N valores aleatórios do tipo int no intervalo [20, 80], onde N é
uma constante dada. O programa deve calcular e imprimir a média (não se preocupe com o
arredondamento), e depois mostrar cada um dos valores, dizendo se ele é maior, menor ou igual à
média. Por fim, o programa deve mostrar quantos dos valores eram maiores e quantos eram menores
do que a média.
*/
/*
Escreva um programa que gera N valores aleatórios do tipo int no intervalo [20, 80], onde N é
uma constante dada. O programa deve calcular e imprimir a média (não se preocupe com o
arredondamento), e depois mostrar cada um dos valores, dizendo se ele é maior, menor ou igual à
média. Por fim, o programa deve mostrar quantos dos valores eram maiores e quantos eram menores
do que a média.
*/
/*

#include<stdio.h>
#include<stdlib.h>
#define N 30

int main(){

  int v[N], i, soma = 0;
  float media;
  srand(time(NULL));

  for(i=0; i<N; i++){
    v[i] = rand()%61+20;
    printf("%d", v);
    }

}
*/
/*
1. Escreva um programa que gera N valores aleatórios do tipo float no intervalo [-40,+40], onde N
é uma constante dada. O programa deve calcular e imprimir a média dos positivos, e depois mostrar
cada um dos valores positivos, dizendo se o valor é maior, menor ou igual à média. Já os valores
negativos não devem ser armazenados em um vetor.
*/
#include<stdio.h>
#define N 40

int main(void){
  for(i=1;i<=20;i++){
    if(val>0){
      vetor[pos] = val;
      soma = soma + vetor[pos];
      post++;
    }
    media = soma/pos
  }
  return (float)

int ehParDeFoolano (unsigned int n1, unsigned int n2){
    v1[n1], v2[n2];
    
    while(n1>0)
    {
        decomposiçao do primeiro valor
            se na decomposicao houver 0 no inicio
                retorna 0
                
        cont[n1%10]++;
    }     
    
    compara se há esses dígitos no outro valor
        se nao houver um caso, retorna 0
            
    }

    
}
