// Mostrar arquivos do diretório
  ls

// Ocultos
  ls -a

// Limpar a tela
  clear

// Mostrar diretório atual
  pwd

// Copiar arquivos
  cp arquivo /diretorio_desejado

// Renomear
  mv nome nome_novo

// Para copiar um diretório
  cp -R /Diretório /Desejado

// Mover
  mv "arquivo" /Desejado

// Remove *arquivos* e *diretórios*
  rmdir /Diretório

// Recursivamente
  rm -r /diretório

// Editor de texto vim
  vim nome_arquivo.extensão
    


// Atualizar lista de pacotes
  sudo apt update

// Procurar pacotes relacionados ao vim
  sudo apt search vim

// Instalar o vim
  sudo apt install vim

// Mostrar versão do vim
  vim --version

// Exibir imagem
  display "imagem.png"

// Encontrar arquivo fulano
  find -name "fulano"

// Extrair arquivo zipado
  unzip "filename"

// Executar
  ./nomearquivo




  

  






// Mostrar o tamanho dos arquivos (MBs)
df -m nomearquivo
du -sh nome_dir
du -sh nome_arq
